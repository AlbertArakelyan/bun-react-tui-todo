{
  "name": "bun-react-tui-todo",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run todo.jsx",
    "build": "bun build todo.jsx --outdir dist --target bun",
    "build:exe": "bun build todo.jsx --compile --outfile todo.exe"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "react-devtools-core": "^7.0.1"
  },
  "dependencies": {
    "ink": "^6.6.0",
    "ink-text-input": "^6.0.0",
    "react": "^19.2.3"
  }
}
